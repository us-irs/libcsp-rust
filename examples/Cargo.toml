[package]
name = "libcsp-rust-examples"
version = "0.1.0"
edition = "2021"

[dependencies]
libcsp-rust =  { path = "../libcsp-rust" }

[build-dependencies]
libcsp-cargo-build = { path = "../libcsp-cargo-build" }
